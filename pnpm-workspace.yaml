packages:
  - "packages/*"

# esbuild ships prebuilt binaries via its postinstall — needed by vite + vitest's
# transformer. Trusted: pinned versions from pnpm-lock.yaml.
allowBuilds:
  esbuild: true
