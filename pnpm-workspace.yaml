packages:
  - "packages/*"
  - "mobile"

# esbuild ships prebuilt binaries via its postinstall — needed by vitest's
# transformer. Trusted: pinned versions from pnpm-lock.yaml.
allowBuilds:
  better-sqlite3: true
  esbuild: true
  unrs-resolver: true
