{
  "name": "notifier",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tail choda-deck queue.jsonl artifacts and push minimal completion events to ntfy.sh.",
  "scripts": {
    "start": "tsx src/cli.ts",
    "dev": "tsx watch src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  }
}
